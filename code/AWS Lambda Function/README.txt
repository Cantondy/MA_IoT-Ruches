#################################
AWS Lambda Function documentation
#################################

The "nodejs" folder is a package for influxDB client library in Javascript, needed by the function "iotCore_to_influxDB".

This folder need to be added as a "layer" in AWS lambda function. 